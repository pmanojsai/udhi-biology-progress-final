# CRITICAL REFACTOR — Udhi Technologies GCSE Biology Assignment

You are taking over an existing implementation of the Udhi Technologies Frontend Developer Intern assignment.

A previous build already created a visually polished and functionally reasonable prototype. DO NOT throw away the good product/UI work.

However, a strict technical audit found that the current implementation is only a **DEMO**, not yet a submission-quality solution.

Your task now is to **refactor and migrate the existing project into a clean, professional, assignment-compliant Next.js application while preserving the strongest existing visual design and product behavior.**

This is a correction/refactor task, not a request to redesign the product from scratch.

---

# 1. NON-NEGOTIABLE ASSIGNMENT REQUIREMENTS

The final project MUST use:

* Next.js
* React
* TypeScript
* Tailwind CSS

It MUST:

* work on desktop
* work on mobile
* correctly handle not-started topics
* use the supplied data exactly
* be deployable to Vercel
* have clean frontend architecture
* be production-buildable

The assignment is specifically testing:

* design judgment
* component structure
* TypeScript
* responsiveness
* edge-case handling
* responsible AI-assisted development

Do not add unnecessary product features.

---

# 2. CURRENT PROJECT AUDIT FINDINGS

A strict read-only audit of the current project found:

## CRITICAL

1. The current project is Vite + React, NOT Next.js.
2. The repository contains unrelated backend/API/database/workspace scaffold.

## IMPORTANT

3. The entire application is concentrated in App.tsx.
4. Business logic is inside the page component rather than separate modules.
5. The original `student + topics` source object shape is not preserved as the source of truth.
6. Recommendation recency uses a hardcoded reference date.
7. Several presentation values are hardcoded instead of derived from the source data.
8. Tailwind is installed but the actual UI is primarily custom CSS rather than meaningful Tailwind usage.
9. There are many unnecessary dependencies.
10. There are unused scaffold files.
11. TypeScript does not enforce unused locals/imports.
12. Mobile navigation accessibility can be improved.
13. Progress indicators should use stronger semantic progressbar accessibility.
14. Replit-specific repository artifacts remain.

The audit also confirmed that the existing implementation has good parts:

* Respiration correctly emerges as the recommendation.
* Transport in Cells correctly emerges as the secondary focus.
* Homeostasis and Inheritance correctly appear as Not Started.
* Average started-topic mastery is correctly calculated as 63%.
* Exam countdown is dynamically calculated.
* Desktop/tablet/mobile layouts are functional.
* Production build currently succeeds.

PRESERVE THESE GOOD BEHAVIORS.

---

# 3. FIRST PRIORITY — MIGRATE TO NEXT.JS

The final application MUST be a real Next.js application.

Do NOT simply install `next` while leaving the Vite architecture in place.

Remove the Vite application architecture and create a proper Next.js App Router structure.

Use a clean structure such as:

```text
src/
  app/
    layout.tsx
    page.tsx
    globals.css

  components/
    StudentHeader.tsx
    ProgressOverview.tsx
    ExamCountdown.tsx
    NextMove.tsx
    TopicOverview.tsx
    TopicGroup.tsx
    TopicCard.tsx
    NotStartedTopic.tsx

  data/
    biology.ts

  lib/
    progress.ts
    dates.ts

  types/
    biology.ts
```

You may improve this structure if there is a genuine reason.

Do not create unnecessary directories or dozens of tiny components.

---

# 4. REMOVE VITE

The final application must not depend on Vite.

Remove:

* Vite configuration
* Vite scripts
* `@vitejs/plugin-react`
* Vite-specific entry architecture
* unnecessary Vite-specific dependencies/configuration

The final scripts should be appropriate for Next.js, for example:

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  }
}
```

Only use scripts supported by the actual installed Next.js version.

Do not invent commands that do not work.

---

# 5. PRESERVE THE EXACT SOURCE DATA SHAPE

Create:

```text
src/data/biology.ts
```

with the original source object structure:

```ts
export const biologyData = {
  student: {
    name: "Priya",
    subject: "Biology",
    examBoard: "AQA",
    examDate: "2027-05-18",
  },
  topics: [
    {
      id: 1,
      name: "Cell Structure",
      mastery: 92,
      questionsAttempted: 40,
      lastStudied: "2026-08-19",
    },
    {
      id: 2,
      name: "Cell Division",
      mastery: 71,
      questionsAttempted: 28,
      lastStudied: "2026-08-14",
    },
    {
      id: 3,
      name: "Transport in Cells",
      mastery: 45,
      questionsAttempted: 12,
      lastStudied: "2026-07-30",
    },
    {
      id: 4,
      name: "Photosynthesis",
      mastery: 88,
      questionsAttempted: 35,
      lastStudied: "2026-08-20",
    },
    {
      id: 5,
      name: "Respiration",
      mastery: 23,
      questionsAttempted: 6,
      lastStudied: "2026-06-11",
    },
    {
      id: 6,
      name: "Homeostasis",
      mastery: 0,
      questionsAttempted: 0,
      lastStudied: null,
    },
    {
      id: 7,
      name: "Inheritance",
      mastery: 0,
      questionsAttempted: 0,
      lastStudied: null,
    },
    {
      id: 8,
      name: "Ecology",
      mastery: 58,
      questionsAttempted: 19,
      lastStudied: "2026-08-02",
    },
  ],
};
```

Do not split the source object into separate top-level student/topics variables.

Create TypeScript types separately if useful, but keep the source data shape unchanged.

---

# 6. MOVE ALL BUSINESS LOGIC OUT OF UI COMPONENTS

Create dedicated utilities.

For example:

```text
src/lib/progress.ts
src/lib/dates.ts
```

Implement clean functions such as:

```ts
getStartedTopics()
getNotStartedTopics()
getTopicStatus()
getRecommendedTopic()
getTopicPriority()
calculateAverageStartedMastery()
getDaysUntilExam()
formatLastStudied()
```

The UI components should consume derived values.

Do NOT place the recommendation formula, date calculations and topic classification inside `page.tsx`.

Avoid unnecessary React state.

Avoid unnecessary `useEffect`.

Use derived values where possible.

---

# 7. RECOMMENDATION LOGIC

Keep the current successful behavior:

Respiration should naturally be the primary recommendation.

Transport in Cells should naturally be the secondary focus.

The recommendation must NOT contain:

```ts
topic.name === "Respiration"
```

or any equivalent hardcoded selection.

It must be derived from:

* mastery
* questions attempted
* recency

Create a transparent heuristic.

The heuristic should reward:

* lower mastery
* fewer questions attempted
* longer time since last study

Do not overengineer this into machine learning.

The result should remain explainable.

---

# 8. FIX THE HARDCODED REFERENCE DATE

The current implementation uses:

```ts
new Date("2026-08-21T12:00:00")
```

inside recommendation logic.

Remove this hidden hardcoded reference date.

Create a centralized date utility.

The recommendation should use a clearly defined current/snapshot date rather than having an unexplained date buried inside the scoring function.

Do not create inconsistent date behavior.

The exam countdown should continue to derive from:

```text
student.examDate
```

and current date.

---

# 9. REMOVE HARDcoded PRESENTATION DATA

Do not duplicate source data in the UI.

Do not manually write:

```text
18 May 2027
AQA GCSE Biology
8 topics
21 August 2026
```

when those values can be derived.

Use:

```ts
biologyData.student.examDate
biologyData.student.examBoard
biologyData.student.subject
biologyData.topics.length
```

If a snapshot/update date is genuinely needed for the UI, make its purpose explicit and centralize it rather than duplicating it throughout components.

Do not introduce fake dynamic data.

---

# 10. NOT-STARTED LOGIC — KEEP THE CURRENT CORRECT BEHAVIOR

This is a critical requirement.

A topic is considered not started when:

```ts
questionsAttempted === 0 && lastStudied === null
```

Therefore:

Homeostasis → Not Started

Inheritance → Not Started

Do NOT display:

> 0% mastery

as though the student has zero knowledge.

The UI should instead communicate:

**Not started**

**No questions attempted yet**

**Start topic →**

Do not classify topics as not-started solely because mastery is 0.

---

# 11. KEEP THE CURRENT GOOD PRODUCT STRUCTURE

Preserve the existing strong information hierarchy:

1. Student context
2. Progress overview
3. Your Next Move
4. Topic landscape
5. Not Started topics

The primary visual focus should remain the student's next recommended action.

Do not redesign the product unnecessarily.

The screen should answer:

> Where do I stand?

and:

> What should I do next?

within a few seconds.

---

# 12. KEEP THE EXISTING VISUAL QUALITY

The current prototype was visually assessed as strong.

Preserve its strongest visual characteristics:

* calm premium education-product aesthetic
* strong typography
* whitespace
* clear hierarchy
* restrained accent color
* polished cards
* clear recommendation area
* responsive layout

Do not replace it with a generic Tailwind template.

The goal is:

**same or better visual quality + significantly better engineering underneath.**

---

# 13. CONVERT THE UI TO MEANINGFUL TAILWIND

The current implementation has Tailwind installed but relies heavily on custom CSS.

Refactor the UI so Tailwind is genuinely used for layout, spacing, responsive behavior, typography, borders, states and most component styling.

Avoid creating a giant custom CSS file that reproduces Tailwind utilities.

Keep `globals.css` for:

* base styles
* CSS variables if useful
* global typography
* unavoidable global rules

Do not use Tailwind just superficially.

---

# 14. COMPONENT ARCHITECTURE

Break the existing `App.tsx` into meaningful components.

At minimum, consider:

```text
StudentHeader
ProgressOverview
ExamCountdown
NextMove
TopicOverview
TopicGroup
TopicCard
NotStartedTopic
```

Each component should have a clear responsibility.

Do not create components that exist only to hold five lines of JSX.

The page should primarily compose these components rather than containing all implementation logic.

---

# 15. TYPESCRIPT QUALITY

Use strict TypeScript.

Enable appropriate compiler checks, including:

```json
"noUnusedLocals": true,
"noUnusedParameters": true
```

if compatible with the project.

Remove:

* unused imports
* unused variables
* unnecessary `any`
* dead types
* dead code

Use explicit types where they improve maintainability.

Do not over-type trivial JSX.

---

# 16. DEPENDENCY CLEANUP

Inspect `package.json`.

Remove dependencies that are not genuinely used by the final application.

Do not keep large template dependencies just because they came from the original workspace.

The final project should have a minimal, defensible dependency set.

Keep only what is actually required.

For example, if the final application only needs:

* next
* react
* react-dom
* lucide-react

and the chosen Tailwind packages/configuration, remove unrelated packages.

Do not remove a package if the final code genuinely uses it.

---

# 17. REMOVE UNRELATED BACKEND/WORKSPACE CODE

This is a frontend assignment.

Remove unrelated:

* API server
* database code
* database configuration
* API client libraries
* API schemas
* workspace-only infrastructure
* unused artifacts
* unrelated generated packages

Specifically inspect/remove things such as:

```text
artifacts/api-server
lib/db
lib/api-spec
lib/api-client-react
lib/api-zod
```

if they are not required by the final Next.js frontend.

The final repository should contain only what is necessary for this assignment.

---

# 18. REMOVE REPLIT-SPECIFIC ARTIFACTS

Before finalizing, search the ENTIRE repository for:

```text
Replit
replit
replit.dev
```

Also inspect:

```text
.replit
replit.md
.local
.conversation
workspace configuration
Replit-specific deployment/configuration
```

Remove Replit-specific files that are not required for the final application or deployment.

Remove visible Replit branding.

Remove Replit-specific comments.

Remove Replit-specific README content.

Do not leave the final project looking like a platform-generated demo.

Do not remove a file if doing so would break the application; first determine whether it is genuinely required.

---

# 19. CLEAN UNUSED FILES

Remove unused scaffold files such as:

```text
src/hooks/use-mobile.tsx
src/lib/utils.ts
components.json
tsconfig.tsbuildinfo
```

if they are not required by the refactored application.

Before deleting, verify they are unused.

Do not leave dead template infrastructure in the repository.

---

# 20. ACCESSIBILITY IMPROVEMENTS

Preserve existing accessibility work and improve it.

For the mobile navigation:

* support Escape to close
* provide appropriate backdrop behavior
* support outside-click dismissal where appropriate
* maintain logical keyboard focus
* restore focus to the menu button after closing if practical

For progress indicators, use semantic progressbar attributes where appropriate:

```html
role="progressbar"
aria-valuenow="..."
aria-valuemin="0"
aria-valuemax="100"
```

Do not rely on color alone.

Keep visible focus states.

Respect reduced motion.

---

# 21. INTERACTIONS

Keep the existing useful local interactions where they make sense:

* topic filters
* review prompts
* not-started state interactions
* practice-session status
* help explanation
* mobile menu

Do not pretend a full learning engine exists.

The assignment only asks for the progress screen.

If the CTA displays a local "session ready" state instead of launching a real question engine, that is acceptable.

Keep interactions honest.

---

# 22. RESPONSIVE TESTING

After refactoring, actually inspect the application at:

* approximately 1440px desktop
* approximately 1024px
* approximately 768px tablet
* approximately 390px mobile

Verify:

* no horizontal overflow
* no broken layout
* readable typography
* CTA remains prominent
* topic names remain readable
* navigation works
* cards stack correctly
* spacing is intentional
* mobile feels designed rather than compressed

Fix genuine issues.

---

# 23. BROWSER VERIFICATION

Do not rely only on TypeScript/build success.

Run the application.

Open it in the browser.

Verify the actual rendered screen.

Test:

### Progress

The UI should show approximately:

**63% average mastery across started topics**

because:

```text
(92 + 71 + 45 + 88 + 23 + 58) / 6 = 63%
```

### Recommendation

Respiration should be first.

Transport in Cells should be the next strongest focus.

### Not Started

Homeostasis and Inheritance must display as Not Started.

### Exam

Countdown must be derived from:

```text
2027-05-18
```

### Interactions

Actually click the controls.

Do not merely inspect their source code.

---

# 24. BUILD VERIFICATION

Run:

* TypeScript/type checking
* lint if configured
* production build

Fix all genuine errors.

Do not ignore errors by weakening the configuration.

Do not hide errors with `any`.

Do not disable strict checks just to make the build pass.

---

# 25. FINAL REPOSITORY STANDARD

When finished, the repository should look like a focused professional frontend project.

It should NOT look like:

* a Replit workspace
* a Vite demo
* a full-stack template
* an AI-generated scaffold
* an unused component library
* a collection of unrelated experiments

It should look like:

> **A deliberately engineered Next.js frontend assignment submitted by a frontend developer.**

---

# 26. IMPORTANT: DO NOT REDESIGN FOR THE SAKE OF IT

The existing visual design and product behavior are already reasonably strong.

Do NOT throw away the current design and create something completely different.

Prioritize:

1. Next.js migration
2. clean architecture
3. exact data shape
4. separated business logic
5. Tailwind implementation
6. dependency cleanup
7. scaffold cleanup
8. Replit cleanup
9. accessibility improvements
10. final responsive verification

Only make visual changes when they improve the existing product.

---

# 27. FINAL ACCEPTANCE CRITERIA

Do not declare the task complete until ALL of these are true:

### Framework

* [ ] Real Next.js application
* [ ] React
* [ ] TypeScript
* [ ] Tailwind CSS
* [ ] No Vite dependency/architecture

### Data

* [ ] Exact original `biologyData` shape preserved
* [ ] No source-data mutation
* [ ] No duplicated hardcoded student/topic data

### Logic

* [ ] Started topics derived
* [ ] Not-started topics derived correctly
* [ ] Topic status derived
* [ ] Average started-topic mastery derived
* [ ] Recommendation derived
* [ ] Recommendation not hardcoded
* [ ] Recommendation recency is not hidden behind an unexplained hardcoded date
* [ ] Exam countdown derived from examDate

### Architecture

* [ ] No giant App.tsx
* [ ] Meaningful components
* [ ] Business logic separated
* [ ] Clean TypeScript
* [ ] No unnecessary state
* [ ] No unnecessary effects

### Styling

* [ ] Tailwind meaningfully used
* [ ] Existing strong visual quality preserved
* [ ] Responsive desktop
* [ ] Responsive tablet
* [ ] Responsive mobile

### Accessibility

* [ ] Keyboard accessible
* [ ] Visible focus
* [ ] Semantic controls
* [ ] Semantic progress indicators
* [ ] Mobile navigation behavior handled properly
* [ ] Reduced-motion support

### Repository

* [ ] No unnecessary backend
* [ ] No database scaffold
* [ ] No unrelated API code
* [ ] No unused dependencies
* [ ] No unused scaffold files
* [ ] No Replit branding/artifacts unless genuinely required by the environment
* [ ] No placeholder/demo text
* [ ] No debug logs

### Verification

* [ ] Browser-tested
* [ ] Desktop-tested
* [ ] Mobile-tested
* [ ] Typecheck passes
* [ ] Production build passes

---

# FINAL INSTRUCTION

Do this as a **real refactor**, not a superficial patch.

First inspect the existing project and identify the affected files.

Then migrate the application to Next.js and perform the architecture cleanup.

Preserve the existing good UI/product work wherever possible.

Do not simply install Next.js and leave the Vite structure underneath.

Do not merely rename files.

Do not merely hide errors.

Do not weaken TypeScript.

Do not add unnecessary features.

Do not invent data.

Do not change the supplied data shape.

When finished, run the actual application, test it in the browser at desktop and mobile sizes, run the production build, and verify the final repository against every acceptance criterion above.

The final result must be a **clean, focused, professional Next.js submission for the Udhi Technologies assignment**, not a Replit/Vite demo.
